# Ignore uploaded files but keep directory structure
uploads/*
!uploads/.gitkeep
!uploads/*/
uploads/*/*.jpg
uploads/*/*.jpeg
uploads/*/*.png
uploads/*/*.gif
uploads/*/*.pdf
uploads/*/*.doc
uploads/*/*.docx

# Keep .gitkeep files
!**/.gitkeep

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
app/logs/*.log

# Temporary files
tmp/
temp/
*.tmp

# Test files
test_header_session_cache.php

# Node.js
node_modules/
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock

# Server data files
server/push-subscriptions.json
server/medications.json
server/settings.json

# Environment variables
# IMPORTANT: Never commit .env with real credentials
.env
.env.local
.env.production

# Server configuration with credentials
# If you add SetEnv directives with real credentials to .htaccess, 
# create a separate .htaccess file and add it here to prevent committing credentials
# Example: 
# .htaccess  (if it contains SetEnv with real credentials)
# Or use .htaccess.local for credentials and merge at deployment

# Composer dependencies
vendor/
composer.lock

# iOS / Capacitor
ios/App/Pods/
ios/App/Podfile.lock
ios/App/build/
ios/App/*.xcworkspace/xcuserdata/
ios/App/*.xcodeproj/xcuserdata/
ios/App/*.xcodeproj/project.xcworkspace/xcuserdata/
ios/App/App/public/
*.ipa
*.dSYM.zip
DerivedData/

